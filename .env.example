# Copy to .env and fill in. Never commit the filled version.

# Where the SQLite database lives (API)
PAXCAST_DB=paxcast.db

# Public HTTPS base URL of the deployed API, inlined into the mobile bundle
# at build time. Must be https:// — Android blocks cleartext by default.
EXPO_PUBLIC_API_BASE_URL=https://your-api.example.com
